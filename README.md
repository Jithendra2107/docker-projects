# Docker Projects

This repository is a collection of Dockerized applications and experiments.  
Each project is self-contained in its own folder with its own Dockerfile, dependencies, and documentation.

## 📂 Projects

- **project1** → A simple Flask web application containerized with Docker.

## 🚀 Getting Started

Clone the repository:
```bash
git clone git@github.com:your-username/docker-projects.git
cd docker-projects
